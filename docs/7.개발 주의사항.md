# UploadTool 개발 시 주의사항

## 개요

UploadTool 개발 시에는 **기존 툴 기능 재현**, **확정 범위 준수**, **고객사 확인 전 추측 구현 금지**, **기존 기능 유지**를 최우선 기준으로 합니다.

특히 LG U+ CDN, 효성 CDN, KT CDN은 API 문서 수령 전까지 실제 Purge API를 추측 구현하지 않고, Stub Adapter 구조까지만 준비합니다.

---

## 범위 관련 주의사항

| 구분 | 주의사항 |
| --- | --- |
| 스토리지 범위 | Tencent COS 지원을 1차 범위처럼 문서화하지 않음 |
| 스토리지 범위 | S3-compatible storage를 1차 핵심 범위처럼 강조하지 않음 |
| 인증 | 인증/로그인을 NexusPurge 자체 계정 DB 방식으로 구현하지 않음 |
| 인증 | 자체 로그인 화면을 새로 만들지 않음 |
| CDN | LG U+ CDN 실제 API 호출을 추측으로 구현하지 않음 |
| CDN | 효성 CDN 실제 API 호출을 추측으로 구현하지 않음 |
| CDN | KT CDN 실제 API 호출을 추측으로 구현하지 않음 |
| 기존 기능 | CloudFront 기존 구현을 깨지 않음 |
| 기존 기능 | Akamai 기존 구현을 삭제하지 않음 |
| 타입/설정 | 기존 타입/설정 필드를 임의로 제거하지 않음 |
| 고객사 확인 | 고객사 확인이 필요한 항목을 확정된 것처럼 실제 구현하지 않음 |
| 미팅 문서 | 미팅 질문 리스트에 인증/로그 상세 항목을 다시 포함하지 않음 |
| 실제 개발 | 실제 개발 구조에는 인증 연동 준비와 로그/결과 처리를 포함함 |

---

## CDN 관련 주의사항

| CDN | 처리 기준 |
| --- | --- |
| CloudFront | 기존 구현 유지 |
| Akamai | 기존 구현 유지, URL Purge / CP Code Purge 방식은 고객사 확인 후 확정 |
| LG U+ CDN | API 문서 수령 전까지 Stub Adapter까지만 구현 |
| 효성 CDN | API 문서 수령 전까지 Stub Adapter까지만 구현 |
| KT CDN | API 문서 수령 전까지 Stub Adapter까지만 구현 |

---

## Stub Adapter 처리 기준

LG U+ CDN, 효성 CDN, KT CDN은 실제 API 문서 수령 전까지 **NotImplemented 에러를 반환하는 Stub Adapter**로만 구현합니다.

| CDN | NotImplemented 메시지 |
| --- | --- |
| LG U+ CDN | `LG U+ CDN purge API is not implemented yet. API specification is required.` |
| 효성 CDN | `Hyosung CDN purge API is not implemented yet. API specification is required.` |
| KT CDN | `KT CDN purge API is not implemented yet. API specification is required.` |

---

## CDN 구현 시 금지 사항

| 항목 | 설명 |
| --- | --- |
| API 추측 구현 금지 | 고객사 문서 없이 Endpoint, Payload, 인증 방식을 임의 구현하지 않음 |
| 인증 방식 추측 금지 | API Key, Token, Signature 방식 등을 임의 가정하지 않음 |
| Rate Limit 추측 금지 | CDN별 호출 제한을 임의로 설정하지 않음 |
| Purge 방식 확정 금지 | URL Purge, Path Purge, CP Code Purge, Wildcard Purge 여부를 문서 없이 확정하지 않음 |
| 기존 구현 훼손 금지 | CloudFront/Akamai 기존 동작을 신규 Provider 추가 과정에서 깨지 않음 |

---

## 인증 관련 주의사항

인증은 UploadTool 자체에서 구현하지 않습니다.

외부 툴 개발업체가 제공하는 인증 모듈을 연동하는 구조로 준비합니다.

| 구분 | 처리 기준 |
| --- | --- |
| 자체 계정 DB | 구현하지 않음 |
| 자체 로그인 화면 | 구현하지 않음 |
| 자체 비밀번호 인증 | 구현하지 않음 |
| 실제 인증 API 호출 | 별도 인증 업체 문서 수령 후 구현 |
| 현재 개발 범위 | 연동 Interface / Stub / 타입 구조 준비 |

---

## 인증 연동 준비 항목

| 항목 | 설명 |
| --- | --- |
| AuthAdapter Interface | 외부 인증 모듈 연동을 위한 공통 Interface |
| ExternalAuthAdapter Stub | 실제 인증 모듈 연동 전 Stub 구현 |
| AuthUser | 사용자 정보 타입 |
| AuthSession | 인증 세션 타입 |
| Token 갱신 Hook | Access Token 만료 및 갱신 대응 구조 |
| Logout Hook | 로그아웃 처리 구조 |
| 사용자 Role 구조 | 사용자 권한 구분 |
| Role 기반 기능 제한 구조 | 기능 및 Profile 접근 제어 기반 |

---

## 로그 관련 주의사항

기본 로그/결과 처리 구조를 포함합니다.

| 구분 | 처리 기준 |
| --- | --- |
| 미팅 질문 | 로그/결과 상세 질문 제외 |
| 실제 개발 | 기본 로그/결과 처리 구조 포함 |
| 저장 방식 | 로컬 JSON 저장 우선 |
| 실패 처리 | 실패 항목 재시도 가능 데이터 보존 |
| CSV Export | 후속 고도화 |
| 감사 로그 | 후속 고도화 |
| 상세 리포트 | 후속 고도화 |
| 재시도 Dashboard | 후속 고도화 |

---

## 로그/결과 처리 포함 범위

| 항목 | 설명 |
| --- | --- |
| 업로드 결과 | 파일별 업로드 성공/실패 기록 |
| 다운로드 결과 | 다운로드 성공/실패 기록 |
| 삭제 결과 | 삭제 성공/실패 기록 |
| 폴더 생성 결과 | 폴더 생성 성공/실패 기록 |
| 이름 변경 결과 | 이름 변경 성공/실패 기록 |
| Purge 결과 | CDN별 Purge 성공/실패 기록 |
| Request ID | CDN API Request ID 저장 가능 구조 |
| Task ID | CDN Purge Task ID 저장 가능 구조 |
| 실패 사유 | 실패 메시지 및 에러 저장 |
| 재시도 데이터 | 실패 항목 재시도에 필요한 데이터 보존 |

---

## 고객사 확인 전 TODO로 남길 항목

| 항목 | 사유 |
| --- | --- |
| LG U+ CDN 실제 Purge API 구현 | API 문서 필요 |
| 효성 CDN 실제 Purge API 구현 | API 문서 필요 |
| KT CDN 실제 Purge API 구현 | API 문서 필요 |
| Akamai CP Code Purge 구현 | 고객사 사용 방식 확인 필요 |
| 삭제 파일 Purge | 고객사 정책 확인 필요 |
| 와일드카드 Purge | CDN별 지원 여부 확인 필요 |
| Query String Purge | 고객사 필요 여부 확인 필요 |
| Purge 완료 상태 Polling | CDN별 상태 조회 API 확인 필요 |
| CDN별 Prefix 차이 | S3-CDN 매핑 규칙 확인 필요 |
| CDN별 Rate Limit | API 문서 및 고객사 운영 기준 확인 필요 |

---

## 검증 기준

작업 완료 후 가능한 범위에서 아래 명령을 실행합니다.

| 명령 | 목적 |
| --- | --- |
| `pnpm typecheck` | TypeScript 타입 검증 |
| `pnpm lint` | Frontend Lint 검증 |
| `cargo check` | Rust Backend 컴파일 검증 |

프로젝트에 해당 스크립트가 없으면 가능한 대체 명령을 사용합니다.

실패 시 실패 이유를 요약하고, 수정 가능한 범위는 수정합니다.

---

## 개발 원칙 정리

> UploadTool 개발은 **기존 툴 기능 재현**을 최우선으로 합니다.
> 
> 
> 고객사 확인이 필요한 항목은 확정된 것처럼 구현하지 않고 TODO로 남깁니다.
> 
> CloudFront와 Akamai 기존 구현은 유지하고, LG U+ CDN, 효성 CDN, KT CDN은 API 문서 수령 전까지 Stub Adapter로만 구성합니다.
> 
> 인증은 자체 구현하지 않고 외부 인증 모듈 연동 구조만 준비하며, 로그/결과 처리는 실제 개발 구조에 포함합니다.
>