# NexusPurge — 추가 기능 후보 목록

> 고객 요구사항 문서(`request.md`) 및 현재 구현 현황(`progress.md`) 기반으로  
> 추가 가능한 기능 전체를 리스트업한 문서입니다.  
> 기준일: 2026-06-18

---

## 범례

| 기호 | 의미 |
|------|------|
| 🟢 | 난이도 낮음 / 단기 구현 가능 |
| 🟡 | 중간 난이도 / 1~3일 소요 |
| 🔴 | 난이도 높음 / 외부 연동 또는 구조 변경 필요 |
| ⭐ | 실제 사용 빈도·임팩트 기준 추천 우선 구현 항목 |

---

## 1. 파일 탐색 / 패널 UX

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 1-1 | **파일 이름 검색 / 필터** (로컬·원격 패널 상단 검색창) | 🟢 ⭐ | 현재 없음; 파일 수가 많을 때 필수 |
| 1-2 | **파일 정렬** (이름 / 크기 / 날짜 오름·내림차순 클릭) | 🟢 ⭐ | 패널 컬럼 헤더 클릭 방식 |
| 1-3 | **폴더 생성** (S3 prefix 생성, 로컬 폴더 생성) | 🟢 | 우클릭 컨텍스트 메뉴 또는 툴바 버튼 |
| 1-4 | **파일/폴더 삭제** (로컬 파일, S3 Object 삭제) | 🟡 | 복수 선택 삭제, Delete 키 지원 |
| 1-5 | **S3 파일 상세 정보 팝업** (Content-Type, ETag, 크기, 스토리지 클래스, 마지막 수정일) | 🟢 | `HeadObject` 한 번 호출로 구현 가능 |
| 1-6 | **Presigned URL 생성** (선택 파일 → 만료 기간 설정 → 공유 링크 복사) | 🟢 ⭐ | 파일 공유 시 매우 자주 사용 |
| 1-7 | **공개 CDN URL 복사** (프로필 CDN 도메인 기반 자동 생성) | 🟢 | 이미 `buildCdnUrl()` 유틸 존재; 버튼만 추가 |
| 1-8 | **즐겨찾기 경로** (로컬/원격 경로 북마크 저장 및 빠른 이동) | 🟡 | localStorage 저장으로 구현 가능 |
| 1-9 | **경로 이력** (뒤로/앞으로 버튼 또는 최근 경로 드롭다운) | 🟡 | 탐색 편의성 향상 |
| 1-10 | **파일 아이콘** (확장자별 아이콘 표시 — 이미지/영상/JS/CSS/JSON 등) | 🟢 | CSS 기반 확장자 매핑으로 구현 가능 |
| 1-11 | **이미지 파일 미리보기** (Presigned URL 기반 팝업 썸네일) | 🟡 | PNG·JPEG·WebP·GIF 대상 |
| 1-12 | **S3 → S3 복사** (원격 패널 내 파일을 다른 prefix로 복사) | 🟡 | `CopyObject` API |
| 1-13 | **패널 비율 조정** (좌/우 패널 경계 드래그 리사이즈) | 🟡 | 현재 50/50 고정 |
| 1-14 | **숨김 파일 표시 토글** (`.`으로 시작하는 파일 숨기기/보이기) | 🟢 | 로컬 패널 옵션 |
| 1-15 | **파일 수·총 용량 표시** (패널 하단 상태바에 선택 정보 표시) | 🟢 | "3개 선택 (12.4 MB)" 형식 |

---

## 2. 전송 기능 고도화

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 2-1 | **전송 일시정지 / 재개** (진행 중인 개별 파일 또는 전체 큐 pause) | 🔴 | Rust Semaphore 제어 필요 |
| 2-2 | **전송 개별 취소** (특정 파일만 취소, 나머지는 계속) | 🟡 | `AtomicBool cancelled` 이미 구조 있음 |
| 2-3 | **전송 속도 제한** (초당 MB 제한 — bandwidth throttle) | 🔴 | tokio `sleep` + 청크 제어 필요 |
| 2-4 | **전송 예약** (특정 날짜·시간에 업로드 실행) | 🔴 | 백그라운드 타이머 또는 OS 스케줄러 연동 |
| 2-5 | **업로드 후 로컬 파일 이동/삭제 옵션** (완료 후 원본 파일 처리 정책) | 🟡 | 업로드 완료 이벤트 시 `fs::remove_file` |
| 2-6 | **동일 이름 처리 정책 선택** (덮어쓰기 / 건너뜀 / 자동 번호 붙이기 / 버전 관리) | 🟡 ⭐ | 현재 overwrite/skip만 있음 |
| 2-7 | **업로드 후 체크섬 검증** (MD5 로컬 계산 vs S3 ETag 비교 확인) | 🟡 | `hash.rs::calculate_md5` 이미 존재 |
| 2-8 | **폴더 재귀 업로드** (하위 폴더 구조 그대로 S3에 반영) | 🟡 ⭐ | 현재 단층 파일만 지원 여부 확인 필요 |
| 2-9 | **S3 스토리지 클래스 선택** (STANDARD / STANDARD_IA / GLACIER 등) | 🟢 | `put_object` 파라미터 추가 |
| 2-10 | **S3 태그 설정** (업로드 시 Key-Value 태그 지정) | 🟢 | `put_object_tagging` API |
| 2-11 | **S3 ACL 설정** (public-read / private 등) | 🟢 | `put_object` `acl` 파라미터 |
| 2-12 | **Multipart 업로드 설정** (분할 기준 크기 설정, 현재 8 MB 기본) | 🟡 | 대용량 파일 안정성 향상 |
| 2-13 | **ETA 및 평균 속도 표시** (전송 큐 / 진행 다이얼로그에 잔여 시간·속도 표시) | 🟢 ⭐ | 현재 bytes/speed 있으나 ETA 계산 미구현 |
| 2-14 | **전송 완료 OS 알림** (백그라운드 전송 완료 시 시스템 토스트 알림) | 🟡 | Tauri `notification` 플러그인 |
| 2-15 | **시스템 트레이 아이콘** (백그라운드 전송 진행 중 트레이에 표시) | 🔴 | Tauri `system-tray` 플러그인 |

---

## 3. CDN / Purge 고도화

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 3-1 | **경로 패턴 Purge** (glob 패턴 직접 입력 — `images/2024/*` 형식) | 🟢 ⭐ | PurgeDialog에 수동 경로 입력 필드 추가 |
| 3-2 | **Purge 이력 조회** (날짜별, 경로별, 성공/실패 필터) | 🟡 | `OperationLog` 데이터 재활용 |
| 3-3 | **Purge 실패 재시도** (실패한 Purge 항목 자동/수동 재시도) | 🟡 | 전송 재시도와 동일 패턴 |
| 3-4 | **CDN 캐시 워밍 / Prefetch** (파일 업로드 후 CDN 엣지 사전 캐시) | 🔴 | CDN Provider별 API 상이 |
| 3-5 | **CDN URL 응답 상세 확인** (상태 코드, 응답 헤더, Cache-Hit 여부) | 🟡 ⭐ | `verify_cdn_urls` 결과 확장 |
| 3-6 | **Purge 예약** (특정 시간에 Purge 실행) | 🔴 | 전송 예약과 동일 구조 |
| 3-7 | **Purge 분할 실행** (배치 크기 초과 시 자동으로 나눠서 순차 Purge) | 🟡 | 현재 경고만 표시; 실제 분할 미구현 |
| 3-8 | **Akamai CDN 어댑터** | 🔴 | `CdnAdapter` trait 구조 이미 준비됨 |
| 3-9 | **LG U+ CDN 어댑터** | 🔴 | 동일 |
| 3-10 | **효성 CDN 어댑터** | 🔴 | 동일 |
| 3-11 | **KT CDN 어댑터** | 🔴 | 동일 |
| 3-12 | **클라우드플레어 어댑터** | 🟡 | Cache-Tag / URL Purge API |
| 3-13 | **Purge 비용 추정** (CloudFront Invalidation 요청 비용 안내) | 🟢 | UI 안내 텍스트로 충분 |

---

## 4. 로그 / 리포트

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 4-1 | **전송 통계 대시보드** (세션별 총 업로드 용량, 파일 수, 소요 시간, 성공률) | 🟡 ⭐ | 로그 패널 내 "통계" 탭 추가 |
| 4-2 | **로그 검색** (키워드, 파일명, 날짜 범위 필터) | 🟡 | 로그 패널 내 검색 입력란 |
| 4-3 | **오류 자동 알림** (OS 알림 또는 이메일) | 🔴 | 이메일은 외부 SMTP 연동 필요 |
| 4-4 | **로그 자동 S3 적재** (수동 호출 없이 전송 완료 시 자동 적재) | 🟡 ⭐ | 현재 `saveOperationLog` 호출은 있으나 적재는 수동 트리거 |
| 4-5 | **로그 보관 기간 설정** (로컬 작업 로그 N일 자동 삭제) | 🟡 | 앱 시작 시 오래된 로그 정리 |
| 4-6 | **일별 / 주별 전송 리포트** (HTML 또는 JSON 파일로 내보내기) | 🟡 | 로그 데이터 집계 후 파일 저장 |
| 4-7 | **Purge 로그 별도 적재** (S3 로그 버킷에 Purge만 따로 prefix 분리) | 🟢 | `OperationLog.purgeResults` 이미 있음 |
| 4-8 | **오류 로그 S3 자동 적재** (오류 발생 즉시 버킷에 기록) | 🟡 | 운영 이슈 추적에 유용 |

---

## 5. 프로필 / 보안

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 5-1 | **프로필 만료일 설정** (특정 날짜 이후 프로필 비활성화) | 🟡 | 프로필 JSON 필드 추가 + 연결 시 검증 |
| 5-2 | **프로필별 기능 권한 제어** (업로드만 가능, Purge 불가, 다운로드 불가 등) | 🟡 ⭐ | 프로필 필드로 UI 요소 숨김/비활성화 |
| 5-3 | **IAM 역할 기반 임시 Credential** (STS `AssumeRole` 지원) | 🔴 | aws-sdk-sts 연동 필요 |
| 5-4 | **프로필 자동 갱신** (토큰 만료 임박 시 재발급 안내 또는 자동 갱신) | 🔴 | 외부 인증 시스템 연동 필요 |
| 5-5 | **세션 타임아웃** (N분 비활동 시 프로필 잠금) | 🟡 | `setTimeout` + 마우스/키 이벤트 감지 |
| 5-6 | **OS 생체인증 연동** (Windows Hello / macOS Touch ID로 프로필 잠금 해제) | 🔴 | Tauri plugin 또는 OS API 연동 |
| 5-7 | **여러 프로필 일괄 Import** (폴더 내 `.nexprofile` 파일 다중 선택) | 🟢 | 파일 선택 다이얼로그 `multiple` 옵션 |
| 5-8 | **프로필 사용 이력** (마지막 연결 시각, 총 업로드 건수 표시) | 🟢 | 프로필 JSON에 통계 필드 추가 |
| 5-9 | **프로필 잠금** (비밀번호 입력 없이 프로필 목록은 보이되 연결 불가 상태) | 🟡 | 앱 재시작 없이 잠금/해제 |
| 5-10 | **MFA(2단계 인증) 지원** (TOTP 코드 입력으로 연결 승인) | 🔴 | 외부 OTP 라이브러리 연동 |

---

## 6. 자동화 / 스케줄

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 6-1 | **폴더 감시 (Watch Mode)** (지정 로컬 폴더 변경 감지 시 자동 업로드) | 🔴 ⭐ | `notify` crate (Rust) 연동 |
| 6-2 | **스케줄 동기화** (cron 표현식으로 주기적 업로드 실행) | 🔴 | 백그라운드 tokio 스케줄러 |
| 6-3 | **업로드 규칙 설정** (확장자별 자동 Content-Type 매핑, 패턴별 Cache-Control 자동 지정) | 🟡 ⭐ | 현재 프로필 기본값 1개; 패턴 규칙 여러 개로 확장 |
| 6-4 | **webhook 알림** (전송 완료 또는 오류 발생 시 URL로 POST 전송) | 🟡 | Slack / Teams / 사내 시스템 연동 |

---

## 7. S3 고급 기능

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 7-1 | **S3 버전 관리 조회** (versioned 버킷에서 이전 버전 목록 확인 및 복원) | 🟡 | `ListObjectVersions` API |
| 7-2 | **S3 버킷 통계** (총 객체 수, 총 용량, 스토리지 클래스별 분포) | 🟡 | `ListObjectsV2` 집계 또는 S3 Storage Lens |
| 7-3 | **S3 수명 주기 규칙 조회** (버킷의 lifecycle 정책 확인 — 읽기 전용) | 🟢 | `GetBucketLifecycleConfiguration` API |
| 7-4 | **S3 Object Lock 표시** (잠긴 파일 표시 및 삭제 경고) | 🟡 | `HeadObject` 응답에 포함 |
| 7-5 | **여러 버킷 프로필 동시 보기** (탭으로 복수 연결 열기) | 🔴 | 상태 관리 구조 변경 필요 |
| 7-6 | **S3 Select (파일 내용 미리보기)** (JSON/CSV 파일 일부 조회) | 🔴 | `SelectObjectContent` API |
| 7-7 | **버킷 간 복사 (Cross-bucket)** (프로필 A 버킷 → 프로필 B 버킷) | 🔴 | 서로 다른 AWS 계정 가능 여부 확인 필요 |

---

## 8. UI / UX

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 8-1 | **키보드 단축키** (F5 새로고침, Del 삭제, Ctrl+U 업로드, Ctrl+D 다운로드, Ctrl+P Purge 등) | 🟡 ⭐ | 파워 유저 필수 기능 |
| 8-2 | **우클릭 컨텍스트 메뉴** (업로드, 삭제, URL 복사, 상세 정보) | 🟡 ⭐ | 패널 파일 우클릭 시 메뉴 |
| 8-3 | **다국어 지원** (한국어 / 영어 전환) | 🟡 | i18n 라이브러리 도입 필요 |
| 8-4 | **테이블 뷰 / 아이콘 뷰 전환** (목록형 ↔ 격자형 토글) | 🟡 | 아이콘 뷰는 이미지 파일에 유용 |
| 8-5 | **컬럼 표시 선택** (파일명 / 크기 / 날짜 / Content-Type 등 컬럼 토글) | 🟡 | 현재 고정 컬럼 |
| 8-6 | **드래그 앤 드롭 파일 순서 변경** (업로드 큐 순서 수동 조정) | 🔴 | 전송 큐 재정렬 |
| 8-7 | **진행 중 파일 상세 팝업** (현재 업로드 중인 파일의 속도·남은 시간·청크 진행) | 🟢 | TransferRow 확장 |
| 8-8 | **전체 진행률 표시** (TitleBar 또는 StatusBar에 전체 진행률 % 표시) | 🟢 | 개별 파일이 아닌 전체 배치 기준 |
| 8-9 | **확인 팝업 줄이기 옵션** ("다시 묻지 않기" 체크박스 추가) | 🟢 | 파워 유저용 |
| 8-10 | **앱 창 크기/위치 기억** (재시작 시 마지막 창 상태 복원) | 🟢 | Tauri `window-state` 플러그인 |
| 8-11 | **접근성 (A11y)** (스크린 리더 지원, 키보드 포커스 개선) | 🟡 | ARIA 레이블 추가 |

---

## 9. 자동 업데이트 / 운영

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 9-1 | **자동 업데이트** (앱 시작 시 새 버전 확인 → 다운로드 → 재시작) | 🔴 ⭐ | Tauri `updater` 플러그인 |
| 9-2 | **업데이트 채널 선택** (stable / beta 채널 전환) | 🔴 | 업데이트 서버 구성 필요 |
| 9-3 | **강제 업데이트 정책** (관리자 설정 버전 이하 사용 차단) | 🔴 | 프로필 서버 연동 필요 |
| 9-4 | **오프라인 모드 감지** (네트워크 없을 때 안내 메시지) | 🟢 | `online` 이벤트 감지 |
| 9-5 | **에러 리포트 수집** (익명 오류 로그를 관리자 서버로 전송 — 선택 동의) | 🔴 | 별도 수집 서버 필요 |

---

## 10. 외부 연동

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 10-1 | **AI LB 프로필 시스템 연동** (인증 서버에서 프로필 자동 발급/갱신) | 🔴 ⭐ | request.md에 명시된 목표 |
| 10-2 | **SSO 연동** (SAML / OIDC 기반 사내 인증) | 🔴 | |
| 10-3 | **HashiCorp Vault 연동** (Credential을 Vault에서 동적으로 가져오기) | 🔴 | |
| 10-4 | **Slack / Teams webhook 알림** (전송 완료, 오류, Purge 결과를 채널에 전송) | 🟡 | webhook URL 프로필에 추가 |
| 10-5 | **이메일 알림** (오류 또는 일별 리포트를 이메일로 발송) | 🔴 | SMTP 설정 필요 |

---

## 11. 통계 / 대시보드

| # | 기능 | 난이도 | 비고 |
|---|------|--------|------|
| 11-1 | **세션 통계 탭** (LogPanel에 "통계" 탭 추가 — 현재 세션의 업로드·다운로드·Purge 건수, 총 용량, 성공률) | 🟢 ⭐ | `transfers` 상태 기반; 별도 API 호출 불필요 |
| 11-2 | **누적 이력 통계** (OperationLog 기반 — 오늘 / 이번 주 / 전체 기간 집계) | 🟡 ⭐ | `list_operation_logs` 커맨드 이미 존재 |
| 11-3 | **업로드 / 다운로드 / Purge 건수 분리 표시** (세션·누적 모두 작업 종류별 분리) | 🟢 | 11-1·11-2 구현 시 함께 구현 |
| 11-4 | **성공 / 실패 / 건너뜀 비율 표시** (파이 차트 또는 가로 바 차트) | 🟡 | 외부 차트 라이브러리 없이 CSS 바로 구현 가능 |
| 11-5 | **총 전송 용량 / 평균 속도** (세션 누적 바이트, 세션 평균 전송 속도) | 🟢 | `transfers` 합산 |
| 11-6 | **CDN Purge 통계** (총 Purge 경로 수, 성공/실패 건수, 마지막 Purge 시각) | 🟢 | `purgeResults` 집계 |
| 11-7 | **오류 유형별 분류** (Network / Auth / S3 / CDN 오류를 카테고리별 카운트) | 🟡 | 오류 메시지 파싱 또는 오류 코드 분류 필요 |
| 11-8 | **프로필별 통계** (어떤 프로필로 얼마나 전송했는지 — 사용 빈도·용량 비교) | 🟡 | OperationLog `profileId` 기반 그룹핑 |
| 11-9 | **일별 전송량 추이 차트** (최근 7일·30일 날짜별 업로드 용량 막대 그래프) | 🟡 | finishedAt 날짜 기준 집계 |
| 11-10 | **통계 CSV / JSON 내보내기** (집계 결과를 파일로 저장) | 🟢 | Blob 다운로드로 구현 가능 |
| 11-11 | **가장 많이 업로드한 파일 유형** (확장자별 업로드 건수·용량 Top N) | 🟡 | `files[].path` 확장자 파싱 |
| 11-12 | **전송 소요 시간 분포** (빠른 파일·느린 파일 분포 — startedAt ~ completedAt 차이) | 🟡 | 타임스탬프 차이 계산 |

---

## 우선 구현 추천 (⭐ 항목 중 상위 10개)

실제 운영 환경에서 즉각적인 효과가 있는 항목:

| 순위 | 항목 | 이유 |
|------|------|------|
| 1 | 1-1 파일 이름 검색/필터 | 파일 수 많을 때 탐색 불가 수준 |
| 2 | 1-2 파일 정렬 | 기본 탐색기 수준 기능 |
| 3 | 1-6 Presigned URL 생성 | 파일 공유 시 매일 사용 |
| 4 | 2-8 폴더 재귀 업로드 | 디렉터리 통째로 업로드 필수 |
| 5 | 2-13 ETA / 평균 속도 표시 | 대용량 전송 시 체감 품질 향상 |
| 6 | 3-1 경로 패턴 Purge | 운영팀이 가장 많이 요청하는 기능 |
| 7 | 11-1 세션 통계 탭 | 업무 보고용, 현재 세션 현황 한눈에 파악 |
| 8 | 11-2 누적 이력 통계 | 기간별 전송 실적 집계 — 보고서 작성 시 활용 |
| 9 | 5-2 프로필별 기능 권한 제어 | 일반 사용자에게 Purge 권한 차단 |
| 10 | 6-1 폴더 감시 (Watch Mode) | 지정 폴더 변경 감지 시 자동 업로드 — 반복 배포 작업 자동화 |
| 11 | 8-1 키보드 단축키 | 파워 유저 생산성 2배 향상 |
